streamlit
streamlit-webrtc
# Usar versão específica do mediapipe que é estável no Linux/Cloud
mediapipe>=0.10.0
# Evitar conflitos de protobuf pinando uma versão compatível
protobuf>=3.20,<4
numpy
Pillow
# OpenCV headless para servidor
opencv-python-headless
av
